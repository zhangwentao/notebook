typescript
==========

+ type annotations - 类型注释

  `let name:string = 'ok';`

+ interface

   ```javascript
	interface test {
		field: string;
	    otherField: number;
	    readonly y: number; // 只读，只能在初始化时赋值
	}
   ```

+ generics 泛型
